{
  "name": "my-studio",
  "version": "1.0.0",
  "private": true,
  "description": "My Studio — ferramenta universal de criação de conteúdo para redes sociais",
  "scripts": {
    "setup": "npm install --prefix tests && npm install --prefix native",
    "dev": "node scripts/dev.js",
    "build": "node scripts/build.js",
    "sync": "npm run build && npm run sync --prefix native",
    "build:android": "npm run sync && cd native/android && ./gradlew assembleDebug",
    "test": "npm test --prefix tests -- ../app/my-studio.html",
    "test:mobile": "npm run test:mobile --prefix tests -- ../app/my-studio.html",
    "test:layout": "npm run test:layout --prefix tests -- ../app/my-studio.html",
    "test:visual": "node scripts/test-visual.js"
  }
}
